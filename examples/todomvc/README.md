# TodoMVC example

Run `npm install` from the root project.

Run `npm run build`  from the root project.

Run `npm install --no-package-lock` from this folder.

Open **index.html** file inside your browser.
