{
  "name": "iglue",
  "version": "0.2.1",
  "description": "Intuitive Data Binding engine",
  "main": "dist/index.js",
  "types": "types/index.d.ts",
  "scripts": {
    "prepublishOnly": "npm run test && npm run build && npm run types",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "test": "mocha -r ts-node/register src/**/*.spec.ts",
    "build": "tsc",
    "types": "tsc --declaration --declarationDir types",
    "check": "tsc --noEmit"
  },
  "author": {
    "name": "Giacomo Gregoletto",
    "email": "gregolettogiacomo@gmail.com"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/chai": "^4.1.3",
    "@types/mocha": "^5.2.0",
    "chai": "^4.1.2",
    "mocha": "^5.1.1",
    "ts-node": "^6.0.3",
    "tslint": "^5.9.1",
    "typescript": "^2.8.1"
  }
}
